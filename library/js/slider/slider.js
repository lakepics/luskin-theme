// Slider JavaScript

(function ($) {

    // where all the actions happens
    $(document).ready(function () {

	    // initializing slider
		$( '.lemmonslider > .slider' ).lemmonSlider({
		    'infinite' : true
		});

    });

})(jQuery);